export enum Languages {
  english = "en-US"
}

export enum PhaserHandlerEvents {
  REMOVE = "remove",
  LOAD = "load",
  RELOAD = "reload"
}

export enum LogTopics {
  ANDESITE = "andesite"
}

export enum RESTMethod {
  GET = 'get',
  POST = 'post',
  USE = 'use',
  PUT = 'put',
  PATCH = 'patch',
  DELETE = 'delete'
}

export enum APIConfig {
  API_PORT = "apiPort",
  API_AUTHORIZATION = "apiAuthorization",
  API_ALLOW_QUERY_AUTH = "apiALlowQueryAuth",
  API_ALLOW_HEADER_AUTH = "apiAllowHeaderAuth",
  API_ENABLED = "apiEnabled"
}